<% if @book.errors.any? %>
  <%= @book.errors.count %>
  <% @book.errors.full_messages.each do |message| %>
     errors prohibited this book from being saved<%= message %>
  <% end %>
<% end %>

<h1>Editing Book</h1>
  <%= form_with model: @book, local:true do |f| %>
    <label>Title</label>
    <%= f.text_field :title %>
    <label>Body</label>
    <%= f.text_area :body %>
    <div class="action">
    <%= f.submit "Update Book" %>
    </div>
  <% end %>
  <%= link_to "Show", book_path(@book.id) %> |
  <%= link_to "Back", books_path(@books) %>